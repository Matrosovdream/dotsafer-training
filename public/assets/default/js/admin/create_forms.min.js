/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "/";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 0);
/******/ })
/************************************************************************/
/******/ ({

/***/ "./resources/js/admin/create_forms.js":
/*!********************************************!*\
  !*** ./resources/js/admin/create_forms.js ***!
  \********************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _createForOfIteratorHelper(o, allowArrayLike) { var it; if (typeof Symbol === "undefined" || o[Symbol.iterator] == null) { if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; var F = function F() {}; return { s: F, n: function n() { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }, e: function e(_e) { throw _e; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var normalCompletion = true, didErr = false, err; return { s: function s() { it = o[Symbol.iterator](); }, n: function n() { var step = it.next(); normalCompletion = step.done; return step; }, e: function e(_e2) { didErr = true; err = _e2; }, f: function f() { try { if (!normalCompletion && it["return"] != null) it["return"](); } finally { if (didErr) throw err; } } }; }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

(function () {
  "use strict";

  $(document).ready(function () {
    var style = getComputedStyle(document.body);
    var primaryColor = style.getPropertyValue('--primary');

    function updateToDatabase(path, idString) {
      console.log(idString);
      $.post(path, {
        items: idString
      }, function (result) {
        if (result && result.title && result.msg) {
          $.toast({
            heading: result.title,
            text: result.msg,
            bgColor: primaryColor,
            textColor: 'white',
            hideAfter: 10000,
            position: 'bottom-right',
            icon: 'success'
          });
        }
      });
    }

    function setSortable(target, moveClass) {
      if (target.length) {
        target.sortable({
          group: 'no-drop',
          handle: '.' + moveClass,
          axis: "y",
          update: function update(e, ui) {
            var sortData = target.sortable('toArray', {
              attribute: 'data-id'
            });
            var path = e.target.getAttribute('data-path');
            updateToDatabase(path, sortData.join(','));
          }
        });
      }
    }

    var items = [];
    var draggableContentLists = $('.draggable-content-lists');

    if (draggableContentLists.length) {
      var _iterator = _createForOfIteratorHelper(draggableContentLists),
          _step;

      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var item = _step.value;
          items.push($(item).attr('data-drag-class'));
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    }

    if (items.length) {
      var _iterator2 = _createForOfIteratorHelper(items),
          _step2;

      try {
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          var _item = _step2.value;
          var tag = $('.' + _item);
          var moveClass = tag.attr('data-move-class');

          if (tag.length) {
            setSortable(tag, moveClass);
          }
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
    }
  });
  $('body').on('change', '#enableLoginSwitch', function () {
    var $card = $('.js-enable-login-fields');

    if (this.checked) {
      $card.removeClass('d-none');
    } else {
      $card.addClass('d-none');
    }
  });
  $('body').on('change', '#enable_welcome_messageSwitch', function () {
    var $card = $('.js-enable-welcome-message-fields');

    if (this.checked) {
      $card.removeClass('d-none');
    } else {
      $card.addClass('d-none');
    }
  });
  $('body').on('change', '#enable_tank_you_messageSwitch', function () {
    var $card = $('.js-enable-tank-you-message-fields');

    if (this.checked) {
      $card.removeClass('d-none');
    } else {
      $card.addClass('d-none');
    }
  });
  $('body').on('change', '.js-form-field-type', function () {
    var value = $(this).val();
    var $titleCard = $('.js-form-field-title-card');
    var $optionsCard = $('.js-field-options');
    $titleCard.addClass('d-none');
    $optionsCard.addClass('d-none');

    if (value) {
      $titleCard.removeClass('d-none');
    }

    if ($.inArray(value, ['dropdown', 'checkbox', 'radio']) !== -1) {
      $optionsCard.removeClass('d-none');
    }
  });
  $('body').on('click', '.js-add-form-field', function (e) {
    e.preventDefault();
    var newFormField = $('#newFormField');
    var key = randomString();
    var html = newFormField.html();
    html = html.replace(/record/g, key);
    $("#formFieldsCard").prepend(html);
  });
  $('body').on('click', '.js-save-form-field', function (e) {
    e.preventDefault();
    var $this = $(this);
    var form = $this.closest('.js-field-form');
    var data = serializeObjectByTag(form);
    var action = form.attr('data-action');
    $this.addClass('loadingbar primary').prop('disabled', true);
    form.find('input').removeClass('is-invalid');
    form.find('textarea').removeClass('is-invalid');
    $.post(action, data, function (result) {
      if (result && result.code === 200) {
        //window.location.reload();
        Swal.fire({
          icon: 'success',
          html: '<h3 class="font-20 text-center text-dark-blue py-25">' + fieldSaveSuccessLang + '</h3>',
          showConfirmButton: false,
          width: '25rem'
        });
        setTimeout(function () {
          window.location.reload();
        }, 500);
      }
    }).fail(function (err) {
      $this.removeClass('loadingbar primary').prop('disabled', false);
      var errors = err.responseJSON;

      if (errors && errors.errors) {
        Object.keys(errors.errors).forEach(function (key) {
          var error = errors.errors[key];
          var element = form.find('.js-ajax-' + key);
          element.addClass('is-invalid');
          element.parent().find('.invalid-feedback').text(error[0]);
        });
      }
    });
  });
  $('body').on('click', '.add-field-option-btn', function (e) {
    e.preventDefault();
    var key = randomString();
    var $parent = $(this).closest('.js-field-options');
    var html = "<li class=\"form-group list-group\">\n                                            <div class=\"input-group\">\n                                                <div class=\"input-group-prepend\">\n                                                    <div class=\"input-group-text cursor-pointer move-icon2\">\n                                                        <i class=\"fa fa-arrows-alt\"></i>\n                                                    </div>\n                                                </div>\n\n                                                <input type=\"text\" name=\"ajax[options][".concat(key, "][title]\"\n                                                       class=\"form-control w-auto flex-grow-1\"\n                                                       placeholder=\"").concat(chooseTitleLang, "\"/>\n\n                                                <div class=\"input-group-append\">\n                                                    <button type=\"button\" class=\"btn remove-field-option-btn btn-danger\"><i class=\"fa fa-times\"></i></button>\n                                                </div>\n                                            </div>\n                                        </li>");
    $parent.find('.js-field-options-lists').append(html);
  });
  $('body').on('click', '.remove-field-option-btn', function (e) {
    e.preventDefault();
    $(this).closest('.form-group').remove();
  });

  function handleGetTitleFromTranslations(translations, defaultLocale) {
    var title = null;

    if (Object.keys(translations).length) {
      Object.keys(translations).forEach(function (key) {
        var translation = translations[key];

        if (translation.locale === defaultLocale) {
          title = translation.title;
        }
      });
    }

    return title;
  }

  $('body').on('change', '.js-form-field-locale', function (e) {
    e.preventDefault();
    var $this = $(this);
    var $form = $(this).closest('.js-field-form');
    var locale = $this.val();
    var path = $this.attr('data-path') + '?locale=' + locale;
    $this.addClass('loadingbar gray');
    $.get(path, function (result) {
      $this.removeClass('loadingbar gray');

      if (result.code === 200) {
        var field = result.field;
        var _locale = result.locale;
        var fieldTitle = '';

        if (field.translations) {
          fieldTitle = handleGetTitleFromTranslations(field.translations, _locale);
        }

        $form.find(".js-title-field-".concat(field.id)).val(fieldTitle);

        if (field.options && field.options.length) {
          Object.keys(field.options).forEach(function (index) {
            var option = field.options[index];
            var optionTitle = '';

            if (option.translations) {
              optionTitle = handleGetTitleFromTranslations(option.translations, _locale);
            }

            $form.find(".js-title-option-".concat(option.id)).val(optionTitle);
          });
        }
      }
    }).fail(function (err) {
      $this.removeClass('loadingbar gray');
    });
  });
  /* feather icons */
  // **
  // **

  feather.replace();
})(jQuery);

/***/ }),

/***/ 0:
/*!**************************************************!*\
  !*** multi ./resources/js/admin/create_forms.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__(/*! /home/jack/projects/webinar/resources/js/admin/create_forms.js */"./resources/js/admin/create_forms.js");


/***/ })

/******/ });